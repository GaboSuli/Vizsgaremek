// SiteUsersPage has been removed per request. This file is kept as a minimal stub so lingering imports do not break the build.
// If you want the file physically deleted from the repository, remove it from the filesystem.

export default function SiteUsersPage() {
  return null;
}
